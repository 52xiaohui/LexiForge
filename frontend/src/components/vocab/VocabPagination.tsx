/**
 * @deprecated Prefer `ListPagination` from `@/components/common/ListPagination`.
 * Kept as a thin re-export so existing imports keep working.
 */
export {
  ListPagination as VocabPagination,
  type ListPaginationProps as VocabPaginationProps,
} from "@/components/common/ListPagination"
