import { ComingSoon } from "@/components/common/ComingSoon"

export function ArticleDetail() {
  return (
    <ComingSoon
      page="文章详情"
      description="正文 + 目标词高亮 + 覆盖率徽章 + Markdown 导出 + 重新生成——下个任务实装。"
    />
  )
}
