import { ComingSoon } from "@/components/common/ComingSoon"

export function Vocab() {
  return (
    <ComingSoon
      page="单词总览"
      description="完整词表、按状态筛选、按 weak_score 排序——下个任务实装。"
    />
  )
}
