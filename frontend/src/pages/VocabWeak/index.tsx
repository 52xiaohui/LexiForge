import { ComingSoon } from "@/components/common/ComingSoon"

export function VocabWeak() {
  return (
    <ComingSoon
      page="薄弱词"
      description="表格 + last_response 筛选 + STICKING 标签 + 勾选生成——下个任务实装。"
    />
  )
}
